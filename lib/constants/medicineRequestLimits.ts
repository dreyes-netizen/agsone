// Company policy: an employee may request at most this many units of a
// medicine in a single request. Shared by the request API route (the real
// boundary) and the request dialog's quantity stepper (UX).
export const MAX_MEDICINE_QUANTITY_PER_REQUEST = 2;
